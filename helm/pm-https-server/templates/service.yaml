apiVersion: v1
kind: Service
metadata:
  name: {{ include "myhttpsserver.fullname" . }}
  labels:
    {{- include "myhttpsserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myhttpsserver.selectorLabels" . | nindent 4 }}
